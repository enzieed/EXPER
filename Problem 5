#include <iostream>
using namespace std;
int main()
{
    int x, y;
    for (x = 1; x <= 10; x++) 
    {
        cout << x << ",";
    }
    
    for (y = x + 1; y <= 30; y+=2)
    {
        cout << y << ",";
    }
    
}
